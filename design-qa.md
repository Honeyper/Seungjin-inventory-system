# DEV PC 관리자 디자인 QA

## 비교 기준

- 선택 시안: `/Users/kang-kyoungmo/.codex/generated_images/019f7ec1-a531-7630-86c5-a5feee995e1a/exec-07d5fd75-9371-4a86-aa22-15ccb2e7870b.png`
- 아이콘 기준 시안: `/Users/kang-kyoungmo/.codex/generated_images/019f7ec1-a531-7630-86c5-a5feee995e1a/exec-8176f1a9-893b-42a2-910a-db2101aa7592.png`
- 아이콘 최종 구현: `/tmp/seungjin-dev-concept3-icons-inbound-latest.png`
- 입고관리 구현: `/tmp/seungjin-dev-live-inbound-final-3.png`
- 재고관리 구현: `/tmp/seungjin-dev-live-inventory.png`
- 출고관리 구현: `/tmp/seungjin-dev-live-shipping.png`
- 제품관리 구현: `/tmp/seungjin-dev-live-products.png`
- 기준 시안 크기: 1536 × 1024 px
- 실제 검증 환경: Chrome PC 창 1440 × 849 logical px, 화면 캡처 2048 × 1152 px

## 비교 이력

1. 초기 혼합안은 선택한 2번 시안보다 사이드바가 넓고 우측 고정 패널 비중이 커서 폐기했다.
2. 2번 시안의 좁은 아이콘형 사이드바, 전체 폭 폼, 얇은 경계선, 흰색 중심 표 레이아웃으로 다시 구현했다.
3. 2번 시안에 빠져 있던 `입고 정보`는 사용자의 요청에 따라 전체 폭 가로 요약 패널로 추가했다.
4. 사이드바, 거래명세서, 불량 사진, 수정, 입고 정보 아이콘을 3번 시안의 얇은 네이비 아웃라인 스타일로 통일했다.
5. 입고관리, 재고관리, 출고관리, 제품관리에서 고정 헤더 겹침, 제목 잘림, 필터 정렬, 표 가독성을 다시 확인했다.
6. 수량 입력 6개와 첨부 2개를 한 줄로 정렬하고, `입고 정보` 제목을 카드 상단으로 분리한 뒤 발주 진행 정보와 등록 버튼 높이를 압축했다.
7. 다중 잔량은 기본 수량 행의 높이를 유지하도록 전체 폭의 `잔량 박스별 수량` 패널로 분리하고, 박스 수와 합계를 즉시 표시하도록 수정했다.
8. 재고관리·출고관리·제품관리의 제목을 입고관리와 같은 관리영역/화면명 구조로 맞추고, 요약 카드·필터·목록을 동일한 흰색 카드와 얇은 경계선 체계로 통일했다.
9. 재고 추가·제품 등록·다중 잔량 삭제는 3번 시안의 Material Symbols 아웃라인 아이콘으로 통일했다.
10. Material Symbols가 시안보다 굵고 둥글게 보여, 3번 시안과 가까운 Tabler 24px 라인 아이콘으로 사이드바·업로드·요약·주의 항목·행 메뉴 전체를 다시 통일했다.
11. 요약 카드 아이콘의 색상 배경 상자를 제거하고, 18~24px 단색 선 아이콘과 동일한 시각 중심선으로 정렬했다.
12. 신규 제품 등록 팝업은 상단 바보다 낮던 레이어 순서를 바로잡고, `헤더 / 입력 영역 / 하단 작업 버튼`을 분리했다. 팝업은 화면 높이를 넘지 않으며 입력 영역만 스크롤되고, 취소·저장 버튼은 항상 하단에 보이도록 고정했다.
13. 제품 기본 정보와 제품 기준 정보를 얇은 경계선의 흰색 섹션으로 재구성하고, Tabler 패키지·측정·저장 아이콘과 압축된 필드 높이로 제품관리 화면의 디자인 체계에 맞췄다.
14. 제품 선택 팝업은 큰 카드형 목록을 컴팩트한 선택 행으로 교체했다. 제품명·거래처를 1차 정보로, 박스당·트레이 수량과 제품코드를 정렬된 2차 정보로 분리하고 검색 결과 수를 추가했다.
15. 제품 선택 팝업도 헤더와 내부 스크롤 영역을 분리하고 화면 높이를 넘지 않도록 수정했으며, 검색·닫기·제품 아이콘을 Tabler 라인 아이콘으로 통일했다.
16. 기존 재고 추가 팝업은 제품 선택을 넓게 배치하고 자동 입력 정보·등록 정보·보관 정보를 3열로 재정렬했다. 기본 정보와 수량 정보는 독립된 흰색 섹션으로 구분하고 작은 화면에서는 2열·1열로 전환한다.
17. 기존 재고 팝업도 헤더와 입력 스크롤, 하단 작업 영역을 분리해 저장·취소 버튼이 항상 보이도록 했으며 검색·창고·박스·저장 아이콘을 Tabler 라인 스타일로 통일했다.
18. 입고관리·재고관리·출고관리·제품관리의 상단 카테고리와 페이지명을 하나의 `management-page-heading` 규격으로 통합했다. 카테고리 열 너비, 구분선, 제목 글자 크기, 제목 영역 높이와 하단 간격을 네 화면에서 동일하게 고정했다.
19. 사이드 메뉴의 포커스 표시는 외곽선을 추가해 크기를 바꾸는 방식 대신 내부선으로 표시해 페이지 전환 후에도 메뉴 폭과 위치가 흔들리지 않도록 했다.
20. 재고 현황의 아이콘은 품목·박스·총 수량·납기 임박의 의미가 즉시 구분되도록 패키지·박스 묶음·수량 스택·일정 경고로 다시 선정했다. 관리 필요 재고도 인쇄·위치 미지정·장기 보관·폐기를 직접 나타내는 아이콘으로 교체하고, 여러 강조색 대신 동일한 네이비 선 굵기와 크기로 통일했다.
21. 출고 결산의 출고대기·이상발견·출고완료 아이콘을 검수 완료 문서·이상 보호 상태·배송 완료 차량으로 교체했다. 작은 원색 아이콘 대신 재고 현황과 동일한 크기의 네이비 Tabler 라인 아이콘을 사용해 화면 간 시각 언어를 통일했다.
22. 입고·출고·제품 상세보기는 상단에 제품명, 거래처, 관리 ID와 핵심 수량·상태를 모은 공통 요약 영역을 추가했다. 기존의 큰 표 형태는 3열 정보 카드로 바꾸고 섹션·헤더·스크롤·하단 작업 영역을 동일한 모달 규격으로 통합했다.

## 최종 확인

- 정적 검증: `node --check frontend/admin.js`, `git diff --check` 통과
- P0: 없음
- P1: 없음
- P2: 배포본의 로그인 후 화면 캡처 재검증 필요
- 의도된 차이: 선택 시안에는 없던 전체 `입고 정보`를 기능 보존을 위해 가로형 패널로 추가했다.
- 아이콘 검증: Tabler Icons 3.31.0의 단색 24px 라인 아이콘으로 교체했으며 사용한 모든 아이콘 클래스가 배포 CSS에 존재함을 확인했다.
- 기능 보존: 기존 입력 ID, 계산 ID, 업로드 버튼 ID, 목록 ID 및 API 연결은 변경하지 않았다.
- 신규 제품 팝업 기능 보존: 기존 `productForm`과 모든 필드·취소·저장 ID는 유지하고 레이아웃 클래스와 아이콘 마크업만 보강했다.
- 제품 선택 기능 보존: 기존 제품 검색·선택 데이터 속성과 입고/기존 재고 분기 로직을 유지하고 결과 수 표시와 정보 위계만 보강했다.
- 기존 재고 기능 보존: 기존 제품 선택, 자동 입력, 필수값, 수량 및 저장 버튼 ID를 유지하고 레이아웃 클래스와 아이콘 마크업만 보강했다.
- 공통 제목 기능 보존: 화면별 제목 문구와 우측 빠른 작업 버튼은 유지하고 공통 레이아웃 클래스만 추가했다.

final result: blocked; authenticated live visual capture pending
## 23. 수정 아이콘 폴백 표시

- 입고 등록의 박스당 수량·검수 수량 등 잠금형 입력란에서 Material Symbols 폰트가 없을 때 `edit` 문자열이 노출되던 문제를 제거했다.
- 기본 상태에서는 기존 연필 SVG를 표시하고, 편집 상태에서는 잠금 SVG로 전환되어 버튼의 의미가 글자 폴백 없이 일관되게 전달된다.

## 24. 팝업 셸 및 스크롤 정리

- 모든 관리자 팝업을 상단 고정 헤더, 독립 스크롤 본문, 하단 고정 작업 영역 구조로 정리하고 관리자 상단바보다 위에 표시되도록 쌓임 순서를 통일했다.
- 신규 제품·기존 재고 팝업의 저장 영역을 본문 스크롤에서 분리해 하단의 과도한 흰 여백을 줄였으며, 제품 선택 검색 아이콘을 입력창 내부에 정렬했다.
- 상세·선택·등록 팝업을 다시 열 때 이전 스크롤 위치가 남지 않도록 초기화하고, 상세 팝업의 프로그램 포커스는 버튼 대신 대화상자에 두어 불필요한 파란 테두리가 노출되지 않게 했다.

## 25. 신규 제품 등록 기능 복구

- 디자인 개편 중 누락된 최종공정, 박가루 제거 유무, 화염처리 유무, 공용용기 제품 입력을 신규 제품 등록과 수정 팝업에 다시 연결했다.
- 공정 옵션은 두 칸 패널로 정리하고 공용용기는 선택했을 때만 출고 제품 종류 수와 제품명 입력이 펼쳐지도록 구성해 기본 화면 높이를 줄였다.
- 등록·수정·상세보기와 DEV 제품 DB 컬럼을 같은 데이터 키로 연결하고, 공용용기 제품명 개수 및 중복 검증을 함께 복구했다.

## 26. 다중 잔량 입력 레이아웃

### 비교 환경

- 원본 문제 화면: `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/TemporaryItems/NSIRD_screencaptureui_5SACNo/스크린샷 2026-07-23 오후 2.39.25.png`
- 수정 구현 화면: `/tmp/seungjin-remainder-layout-viewport.png`
- 전후 비교 화면: `/tmp/seungjin-remainder-comparison-1600.png`
- 원본 픽셀 크기: 2698 × 518 px
- 구현 픽셀 크기: 2698 × 900 px
- 구현 CSS 뷰포트: 2698 × 900 px, device scale factor 1
- 비교 상태: 입고관리 `잔량 박스 다수` 선택, 잔량 박스 2개 표시

### 전체 화면 및 집중 영역 비교

- 전체 화면에서는 기본 수량 행과 첨부 카드의 높이·열 배치를 유지하면서 다중 잔량 패널만 다음 행으로 확장되는 것을 확인했다.
- 집중 영역에서는 수정 전 브라우저 기본 입력 모양과 과도하게 벌어진 단위·삭제 버튼 간격이 제거되고, 각 잔량을 동일한 높이의 흰색 입력 카드로 묶은 것을 확인했다.
- 잔량 입력 목록은 PC에서 2열, 700px 이하에서 1열로 전환되며 1280px와 700px 모두 가로 넘침이 발생하지 않았다.

### 검증 결과

- 글꼴·위계: 기존 관리자 폰트와 라벨 굵기를 유지하고 잔량 번호를 보조 라벨 크기로 통일했다.
- 간격·레이아웃: 입력, `ea`, 삭제 버튼을 하나의 카드 안에 고정하고 목록 최대 폭을 제한해 넓은 화면의 과도한 공백을 줄였다.
- 색상·토큰: 기존 네이비 텍스트, 회색 경계선, 흰색 카드, 골드 추가 버튼 토큰을 그대로 사용했다.
- 이미지·아이콘: 새 이미지 자산은 없으며 기존 Tabler 삭제·추가 아이콘을 유지했다.
- 문구: `잔량 박스별 수량`, 안내 문구, 합계, 추가·삭제의 의미를 변경하지 않았다.
- 상호작용: 잔량 박스 추가 시 2개에서 3개로 증가하고, 3번 삭제 후 다시 2개로 복귀하며 합계와 번호가 정상 갱신됨을 확인했다.
- 반응형: 1280px에서 2열 카드 폭 564px, 700px에서 1열 카드 폭 543px로 렌더링되며 문서 가로 넘침이 없었다.
- 콘솔 오류: 없음.

### 비교 이력

1. P1: 잔량 입력이 일반 폼 스타일을 상속받지 못해 기본 브라우저 입력으로 표시되고 `ea`와 삭제 버튼이 카드 끝까지 벌어졌다.
2. 수정: 잔량별 입력을 독립 카드로 만들고 입력 높이·테두리·단위 위치를 일반 수량 필드와 동일하게 맞췄다.
3. 수정 후: PC 2열, 좁은 화면 1열 전환과 추가·삭제 동작을 재확인했으며 남은 P0/P1/P2 문제는 없다.

final result: passed


## 27. 관리자 로그인 화면 및 참고 컴포넌트 적용

### 비교 환경

- 원본 문제 화면: `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/TemporaryItems/NSIRD_screencaptureui_Od9VrF/스크린샷 2026-07-23 오후 3.30.34.png`
- 참고 프롬프트: `/Users/kang-kyoungmo/.codex/attachments/191e596a-2916-4687-872b-163876351c1f/pasted-text.txt`
- 수정 PC 화면: `/private/tmp/seungjin-login-reference-adapted.png`
- 수정 모바일 화면: `/private/tmp/seungjin-login-reference-mobile.png`
- 원본 화면 크기: 2808 × 1328 px @2x
- 구현 CSS 뷰포트: 1404 × 664 px, device scale factor 1
- 비교 상태: 로그아웃, 빈 입력 상태와 입력 완료 상태

### 디자인 적용

- 기존의 큰 홍보 배너와 과도하게 큰 로그인 영역을 좌측 업무 소개와 우측 집중형 로그인 카드로 분리했다.
- 참고 컴포넌트의 작은 카드, 입력 완료 체크, 오류 흔들림, 인라인 오류 안내, 버튼 눌림 반응과 로딩 상태를 현재 정적 HTML 구조에 맞게 구현했다.
- 회원가입용 다단계 진행, 아바타, 비밀번호 강도 기능은 관리자 로그인 목적과 맞지 않아 제외하고 기존 계정 ID·비밀번호 인증 흐름을 유지했다.
- 로그인 카드는 흰색 표면, 얇은 경계선, 제한된 그림자, 네이비 주 버튼으로 관리자 화면의 디자인 토큰과 통일했다.
- 입력 필드는 완료 시 체크 아이콘이 표시되고 누락 제출 시 해당 필드가 흔들리며 필드별 안내 문구가 펼쳐진다.

### 검증 결과

- PC: 1404 × 664 화면에서 로그인 카드와 저작권 문구가 잘림 없이 표시되고 가로·세로 불필요한 스크롤이 없다.
- 모바일: 390 × 844에서 좌우 넘침 없이 한 열로 전환되며 로그인 카드 내부 입력과 버튼을 사용할 수 있다.
- 상호작용: 계정 ID·비밀번호 입력 완료 표시, 비밀번호 보기/숨기기, 빈 값 제출 시 첫 오류 필드 포커스, 로딩 아이콘 전환을 확인했다.
- 접근성: 입력별 설명 연결, 오류 상태 `aria-invalid`, 비밀번호 보기 버튼의 상태별 이름, 오류 메시지의 `aria-live`를 유지했다.
- 콘솔 오류: 없음.

### 비교 이력

1. P2: 원본 화면은 장식과 큰 제목의 비중이 높아 관리자가 실제 로그인 폼을 빠르게 찾기 어려웠다.
2. 수정: 좌측 브랜드 설명을 업무 중심으로 줄이고 우측 로그인 폼을 작은 단일 카드로 집중시켰다.
3. 참고 프롬프트 반영: 완료 체크·오류 흔들림·필드 안내·버튼 반응을 추가하되 회원가입 전용 기능은 제거했다.
4. 수정 후: PC와 모바일에서 레이아웃, 입력 상태, 오류 상태, 비밀번호 보기 동작을 재확인했으며 남은 P0/P1/P2 문제는 없다.

final result: passed

## 32. 로그인 소개 문구 간소화

- `SEUNGJIN OPERATIONS`, `현장 업무를 하나의 흐름으로.`, 부연 설명 문구를 제거했다.
- 하단에 중복 노출되던 `승진 통합 관리 시스템`을 왼쪽 소개 영역의 메인 헤드카피로 올렸다.
- 기능 카드 3개는 유지하고 로고·헤드카피·기능 카드 사이의 간격을 다시 맞췄다.
- 좁은 화면과 낮은 높이 화면에서도 헤드카피가 자연스럽게 두 줄로 유지되도록 확인했다.

final result: passed

## 29. 신규 제품 공정 옵션 정렬

- `박가루 제거`와 `화염처리`가 최종공정 오른쪽의 좁은 한 칸 안에서 다시 분할되던 전체 폭 지정 오류를 수정했다.
- 제품 등록 폼의 `full-span` 요소가 종류와 무관하게 두 열 전체를 사용하도록 범위를 바로잡았다.
- 두 공정 옵션은 다음 행 전체 폭에 나란히 배치하고, 각 카드 안에서 항목명과 `유 / 무` 선택지가 한 줄로 정렬되도록 변경했다.
- `fieldset / legend`의 브라우저별 축소 동작을 제거하고 접근 가능한 `role="group"` 구조로 교체했다.
- 공용용기 옵션도 의도한 전체 폭을 사용하도록 동일한 전체 폭 규칙을 적용했다.
- 기존 라디오 이름과 값은 유지해 저장 데이터와 동작에는 변화가 없다.

final result: passed

## 30. 출고 목록 정렬

- 기본 정렬을 `최근 출고 변경순 · 내림차순`으로 설정해 출고대기 등록, 검수 수정, 출고 처리 등 출고 관련 데이터가 최근에 변경된 항목을 먼저 표시한다.
- 출고 관련 수정 시각을 DEV 박스관리 DB에 기록하고 목록 API에 포함해 단순 입고일이 아닌 실제 출고 작업 시점으로 정렬한다.
- 기존 데이터는 출고일·출고시간, 검수일·검수시간을 순서대로 사용하고 출고 이력이 없을 때만 입고일을 보조 기준으로 사용한다.
- 사용자가 제품명순, 거래처명순, 입고일순을 선택할 수 있고 오름차순·내림차순 버튼으로 방향을 즉시 바꿀 수 있도록 했다.
- 초기화 시 검색·필터와 함께 정렬도 `최근 출고 변경순 · 내림차순`으로 복원한다.

final result: passed

## 28. 다중 잔량 자동 줄바꿈

- 잔량 입력 목록을 고정 2열에서 `auto-fill` 기반 반응형 그리드로 변경했다.
- 각 입력 카드의 최소 너비는 16rem으로 유지하고, 남는 가로 공간에는 다음 잔량 카드가 우측으로 이어진다.
- 카드 최소 너비를 확보할 수 없는 해상도에서만 자동으로 다음 줄로 이동하며 720px 이하에서는 기존처럼 1열을 유지한다.
- 입고 등록과 입고 수정의 다중 잔량 입력에 같은 규칙을 적용했다.
- 검수 화면에서 잔량 카드 5개를 기준으로 1600px에서는 5열, 1000px에서는 3열, 700px에서는 1열로 자동 전환되는 것을 확인했다.
- 각 해상도에서 카드 순서는 왼쪽에서 오른쪽으로 이어지고 공간이 부족해질 때 다음 줄로 이동하며 가로 넘침이 없다.

final result: passed

## 31. 출고 목록 관리 액션 정리

### 비교 환경

- 원본 문제 화면: `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/TemporaryItems/NSIRD_screencaptureui_9YAnPD/스크린샷 2026-07-23 오후 3.54.31.png`
- 수정 구현 화면: `/private/tmp/seungjin-shipping-actions-open-final.png`
- 원본 화면 크기: 1420 × 192 px
- 구현 CSS 뷰포트: 1280 × 720 px, device pixel ratio 2
- 비교 상태: 일부 출고 행, 보조 작업 메뉴 펼침

### 디자인 적용

- 관리 열에서 `수정`과 `출고`가 세로로 쌓이던 구성을 없애고, 현재 단계의 핵심 행동인 `출고`만 바로 노출했다.
- `수정`, `출고 건 수정`, `추가 출고`, `재검수`, `사진 보기`처럼 빈도가 낮은 작업은 우측 `…` 메뉴로 묶었다.
- 메뉴는 테이블 폭을 늘리지 않는 고정 레이어로 표시되며 화면 하단 공간이 부족하면 버튼 위로 자동 배치된다.
- 출고 버튼은 77 × 31px, 보조 메뉴 버튼은 32 × 31px로 높이를 맞추고 관리 열의 최소 폭을 8rem으로 고정했다.
- 보조 작업에는 기존 Tabler 라인 아이콘을 사용해 다른 관리자 행 메뉴와 시각 언어를 통일했다.

### 검증 결과

- 행 높이: 보조 메뉴를 포함해 48px로 유지되며 두 줄 버튼 때문에 행이 커지지 않는다.
- 상호작용: `…` 버튼의 확장 상태와 메뉴 항목 포커스가 정상 갱신되고, 바깥 클릭·Escape·창 크기 변경·스크롤 시 메뉴가 닫히도록 연결했다.
- 기능 보존: 출고, 수정, 추가 출고, 재검수, 보류 상세, 사진 보기의 기존 동작 경로와 데이터 속성을 유지했다.
- 반응형: 1101~1360px PC 구간에서도 출고 버튼이 지나치게 축소되지 않고 메뉴 버튼과 같은 높이를 유지한다.
- 콘솔 오류: 컴포넌트 검증 화면에서 없음.

### 비교 이력

1. P1: 수정과 출고 버튼이 한 셀에서 세로로 쌓여 다른 행보다 높아지고 표의 수평 리듬이 깨졌다.
2. 수정: 즉시 수행할 출고만 노출하고 나머지 작업을 `…` 메뉴로 이동했다.
3. 수정 후: 행 높이 48px, 두 버튼 높이 31px, 메뉴 열림 상태와 콘솔 오류 없음을 다시 확인했다.

final result: passed

## 33. 제품 활성 옵션 하이라이트

- 박가루 제거와 화염처리는 `유`를 선택했을 때만 해당 선택 박스를 옅은 노란색으로 강조한다.
- `무`를 선택한 상태에서는 라디오 선택 표시만 유지하고 박스 배경은 흰색으로 표시한다.
- 사용 여부는 `사용중`일 때 필드 전체를 공용용기 선택 상태와 같은 노란색 테두리·배경으로 강조한다.
- `미사용` 상태에서는 중립적인 회색 배경을 유지한다.

final result: passed

## 34. 입고 합계 글자 정렬

- `박스 총 수량`과 `입고 총 수량`을 동일한 2열 행 그리드로 맞췄다.
- 수치는 고정된 숫자 열에서 오른쪽 정렬하고 `box`, `ea` 단위는 별도 열에서 왼쪽 정렬해 두 행의 기준선을 통일했다.
- 라벨·수치·단위의 세로 중앙과 줄높이를 맞춰 숫자가 위쪽으로 치우쳐 보이지 않도록 했다.

final result: passed

## 35. 출고 목록 정렬 라벨 정렬

- 다른 검색 필터와 같은 체계를 사용하도록 `정렬` 라벨을 정렬 기준 선택창 위로 이동했다.
- 정렬 기준 선택창, 오름차순·내림차순 버튼, 초기화, 페이지 표시 수 선택창은 같은 하단 기준선에 맞췄다.
- 정렬 기준과 방향을 바꾸는 기존 기능 및 요소 ID는 그대로 유지했다.

final result: passed

## 36. 일부 출고 상세보기 경로

- 일부 출고이면서 남은 박스가 있는 행은 기존 `추가 출고` 버튼을 주 작업으로 유지했다.
- 같은 관리 셀의 `…` 메뉴에 `상세보기`를 추가해 기존 출고 내역과 남은 박스 정보를 확인할 수 있도록 했다.
- 상세보기는 기존 출고 상세 모달을 재사용하며 추가 출고 처리 로직은 변경하지 않았다.

final result: passed

## 37. 출고 목록 관리 버튼 높이

- PC 공통 접근성 규칙이 출고 관리 버튼만 40px로 키우던 충돌을 제거했다.
- `출고대기 등록`, `수정`, `출고`, `추가 출고`, `상세`와 `…` 버튼을 31.2px 높이로 통일했다.
- 관리 버튼이 행 높이를 불필요하게 늘리지 않으며 다른 상태 칸과 같은 세로 중앙에 배치되도록 했다.

final result: passed

## 38. 출고 완료 물량 컬럼

- 출고 목록의 출고 가능 수량 다음에 `출고한 박스`, `출고한 수량 (EA)` 컬럼을 추가했다.
- 출고 완료 상태인 박스 배열을 기준으로 박스 수와 수량 합계를 계산하며 미출고 항목은 각각 `0 box`, `0 ea`로 표시한다.
- 열 추가로 이동한 검수·이상·출고 상태·관리 셀의 동작 참조 위치를 함께 보정했다.

final result: passed

## 39. 출고 목록 조회기간 선택 적용

- 출고 결산의 조회기간은 기존처럼 상단 결산 수치에 항상 적용한다.
- `해당 조회기간만 조회`를 체크한 경우에만 같은 기간 기준을 아래 출고 목록에도 적용한다.
- 기본값은 체크 해제 상태로 유지해 기간과 관계없이 출고 예정 항목을 계속 확인할 수 있다.
- 체크 상태에서 시작일·종료일 또는 오늘 버튼을 변경하면 목록도 즉시 첫 페이지부터 다시 표시한다.
- 목록 초기화 시 기간 한정 조회도 함께 해제한다.

final result: passed

## 40. 출고 가능 항목 상세보기

- `출고` 버튼이 표시되는 출고대기·일부 출고 항목의 `…` 메뉴에 `상세보기`를 추가했다.
- 기존 `출고 건 수정` 또는 `추가 출고` 메뉴와 함께 상세보기가 노출되며, 기존 출고 상세 모달을 그대로 사용한다.
- 출고 처리와 수정 동작은 변경하지 않았다.

final result: passed

## 41. 모바일 홈·출고 관리·재고 수정 통합 디자인

### 비교 환경

- 참고 디자인: `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/codex-clipboard-b6b2c48d-4865-4df6-bd3e-c04657973efb.png`, `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/codex-clipboard-0796f05c-17de-4c48-be26-a9d4e6a652e9.png`, `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/codex-clipboard-aabf1f36-912d-4599-b930-ba88912f6544.png`, `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/codex-clipboard-286bc5cd-869f-4fad-ba66-c81f3f7fad94.png`
- 구현 캡처: `/private/tmp/seungjin-mobile-home-top.png`, `/private/tmp/seungjin-mobile-shipping-v2.png`, `/private/tmp/seungjin-mobile-shipping-empty.png`, `/private/tmp/seungjin-mobile-inventory.png`
- 나란히 비교 캡처: `/private/tmp/seungjin-ui-comparison-wide.png`, `/private/tmp/seungjin-ui-comparison-shipping.png`, `/private/tmp/seungjin-ui-comparison-empty.png`, `/private/tmp/seungjin-ui-comparison-inventory.png`
- 구현 CSS 뷰포트: 390 × 844 px
- 비교 상태: 홈 상단 및 메뉴, 출고 제품 3건, 출고 빈 화면, 재고 수정 제품 1건

### 디자인 적용

- 홈, 출고 관리, 재고 수정 화면을 흰색 배경과 네이비·골드 강조색, 1px 회색 테두리, 얕은 그림자 체계로 통일했다.
- 홈에는 환영 영역, 오늘 출고·완료 박스·입출고 상태 요약, 기능별 메뉴 카드를 적용하고 준비 중인 기존 메뉴는 비활성 상태를 유지했다.
- 출고 화면은 검색, 오늘 날짜·시간, 수동 출고 등록, 총 건수·새로고침·완료 박스·필터 순서를 유지하고 제품별 카드에 업체명, 제품명, 차수·최종공정, 박스 수, 출고 가능 수량, 상태, 주 작업을 배치했다.
- 상태 표기는 `출고대기`, `부분출고`, `보관`, `출고완료` 규칙으로 정규화했으며 `일부 출고`는 `부분출고`로 표시한다.
- 카드 이미지 영역은 현재 Tabler 공장 아이콘을 기본값으로 사용하며 제품 이미지 URL 필드가 추가되면 사진을 `cover` 방식으로 표시하고 로드 실패 시 기본 아이콘으로 복귀한다.
- 재고 수정 카드는 같은 제품 헤더 구조를 사용하고 현재 위치, 이동 위치, 스캔·전체 박스, 현재 수량, 자리이동·전량 이동을 한 카드에서 확인하도록 정리했다.
- 하단 메뉴는 홈, 출고 관리, 작업완료현황, 재고 현황 조회, 재고 수정의 기존 5개 흐름을 유지했다.

### 기능 및 반응형 검증

- 기존 출고 QR 스캔, 수동 등록, 새로고침, 완료 박스 토글, 필터, 출고, 추가 출고, 박스 추가, 출고대기 등록·취소, 수량 변경, 등록 취소, 상세정보 데이터 속성을 유지했다.
- 기존 재고 수정 QR 스캔, 보관 장소 선택, 자리이동, 전량 이동, 사출재고 등록, 등록 취소 데이터 속성을 유지했다.
- 출고 카드 `…` 메뉴를 실제로 열어 5개 보조 작업이 카드 바깥으로 잘리지 않고 표시되는 것을 확인했다.
- 390 × 844 px에서 홈 카드, 출고 상태 배지·수량·주 작업, 재고 수정 위치 선택·수량·버튼이 가로로 겹치지 않았다.
- 370px 이하 전용 규칙에서 화면 좌우 여백, 카드 이미지, 상태 배지, 출고 버튼 크기를 추가로 축소하도록 적용했다.
- `node --check frontend/mobile/mobile.js`와 `git diff --check`를 통과했다.
- 브라우저 콘솔 경고·오류: 없음.

### 비교 이력

1. P1: 기존 홈 카드의 과거 그리드 위치 규칙이 새 카드 레이아웃과 충돌해 아이콘·본문·배지가 세로로 분리됐다.
2. 수정: 홈 화면 전용 열·행 위치를 명시하고 화면 레이아웃을 블록 흐름으로 고정했다.
3. P1: 기존 수동 출고 등록 버튼 배경 규칙이 남아 흰 배경 위에 흰 제목이 표시됐다.
4. 수정: 출고 화면 전용 선택자로 네이비 배경과 흰 제목·보조 설명을 함께 고정했다.
5. 수정 후: 네 화면을 참고 이미지와 같은 비교 페이지에서 다시 확인했고 기능 요소 겹침과 콘솔 오류가 없음을 확인했다.

final result: passed

## 42. 모바일 생성형 얇은 아이콘·홈 카드 상태 표현

### 비교 환경

- 참고 디자인: `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/codex-clipboard-b6b2c48d-4865-4df6-bd3e-c04657973efb.png`
- 사용자 피드백 화면: `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/TemporaryItems/NSIRD_screencaptureui_zq61BG/스크린샷 2026-08-07 오전 11.59.20.png`
- 구현 캡처: `/private/tmp/seungjin-mobile-generated-icons-final.png`
- 구현 CSS 뷰포트: 390 × 844 px

### 디자인 적용

- 참고 이미지의 얇고 정돈된 네이비 아웃라인 스타일을 기준으로 홈 메뉴, 하단 내비게이션, 사용자, 화살표 아이콘 10종을 이미지 생성해 투명 PNG로 적용했다.
- 홈의 사용 가능·사용 불가 카드는 모두 흰색 배경, 중립 회색 테두리, 동일한 아이콘 색으로 통일했다.
- 카드 우측 상태만 `이용 가능`은 골드, `이용 불가`는 회색 배지로 구분해 기능 상태가 색상에 의해 명확히 전달되도록 했다.
- 기존 카드 클릭과 비활성 상태, 하단 내비게이션 버튼의 동작 및 접근성 레이블은 그대로 유지했다.

### 기능 및 반응형 검증

- 390 × 844 px에서 아이콘, 제목, 설명, 상태 배지, 화살표가 겹치지 않고 카드 내부 정렬이 유지됐다.
- 370px 이하에서는 생성 아이콘 크기가 36px로 축소되도록 보정했다.
- 생성 PNG 10종의 투명 배경과 알파 영역을 확인하고, 브라우저 콘솔 오류가 없음을 확인했다.
- `node --check frontend/mobile/mobile.js`와 `git diff --check`를 통과했다.

final result: passed

## 43. 모바일 홈 카드 텍스트 줄바꿈·간격

- 구현 캡처: `/private/tmp/seungjin-mobile-copy-wrap-390.png`
- 검증 너비: 390px
- 제목과 설명 사이 간격을 8px로 조정했다.
- 제목에는 균형 줄바꿈, 설명에는 단어 단위 줄바꿈을 적용해 `수량`, `예외 사항` 등의 단어가 음절 중간에서 갈라지지 않도록 했다.
- 설명의 기존 두 줄 제한과 말줄임, 카드 높이, 상태 배지 및 메뉴 동작은 유지했다.
- 브라우저 콘솔 오류가 없고 4개 카드의 제목·설명·상태 배지가 겹치지 않음을 확인했다.

final result: passed

## 44. 모바일 제품 이미지 미등록 자리 표시자

- 구현 캡처: `/private/tmp/seungjin-product-image-placeholder-390.png`
- 검증 너비: 390px
- 제품 이미지가 없을 때 표시하던 네이비 공장 아이콘을 제거했다.
- 연한 회색 배경과 점선 테두리, 사진 비활성 아이콘, `이미지 없음` 문구로 이미지 미등록 상태를 명확히 표시했다.
- 유효한 제품 이미지 URL이 있으면 기존처럼 카드 영역을 실제 사진으로 채우며, 이미지 로드에 실패한 경우에도 동일한 미등록 자리 표시자로 복귀한다.
- 브라우저 콘솔 오류가 없고 업체명·제품명·출고 상태와 자리 표시자가 겹치지 않음을 확인했다.

final result: passed

## 45. 모바일 관리 화면 글자 굵기

- 구현 캡처: `/private/tmp/seungjin-mobile-balanced-type-390.png`
- 검증 너비: 390px
- 화면 제목과 주요 수량은 800, 제품명·버튼·상태 배지는 700, 업체명·보조 라벨은 600으로 낮춰 정보 위계를 정리했다.
- 검색 입력은 500으로 낮추고 수동 출고, 날짜, 필터 등의 조작 텍스트는 700으로 통일했다.
- 출고 관리와 재고 수정에서 공유하는 카드·버튼 굵기를 함께 조정해 화면 간 일관성을 유지했다.
- 브라우저 콘솔 오류가 없고 텍스트 폭·줄바꿈·버튼 정렬 변화가 없음을 확인했다.

final result: passed

## 46. 모바일 출고 카드 하단 중심 정렬

- 구현 캡처: `/private/tmp/seungjin-centered-card-footer-390.png`
- 검증 너비: 390px
- 등록 박스 수, 출고 가능 수량, 출고 버튼 영역을 2:1 구조로 재배치해 내부 세 칸의 중심선을 동일한 3등분 축에 맞췄다.
- 출고 버튼은 오른쪽 1/3 영역 안에서 좌우 여백이 같도록 가운데 정렬하고 최대 폭 96px을 유지했다.
- 제품 상세정보 행의 좌우 패딩을 제거해 카드 본문 경계선과 같은 시작·끝 기준선에 맞췄다.
- 브라우저 콘솔 오류가 없고 수량 라벨·단위·버튼·상세정보 행이 카드 밖으로 넘치지 않음을 확인했다.

final result: passed

## 47. 모바일 목록 스크롤 점진형 블러

- 스크롤 중 구현 캡처: `/private/tmp/seungjin-scroll-edge-veil-mid.png`
- 검증 너비: 390px
- 참고 프롬프트의 `backdrop-filter`와 그라데이션 `mask-image` 방식을 기존 HTML·CSS·JavaScript 구조에 맞게 적용했다.
- 출고 관리와 재고 수정 목록의 위·아래 가장자리에 52px, 6px 강도의 점진형 블러와 흰색 표면 틴트를 적용했다.
- 목록 최상단에서는 위 블러를 숨기고, 아래에 더 볼 내용이 있을 때만 아래 블러를 표시한다.
- 아래로 스크롤하면 위 블러가 나타나고 목록 마지막에 도달하면 아래 블러가 사라지도록 실제 스크롤 위치를 감지한다.
- 블러 레이어는 포인터 입력을 가로막지 않으며 모션 감소 환경에서는 전환 애니메이션을 제거했다.
- 초기·중간·끝 스크롤 위치에서 상태 전환과 콘솔 오류가 없음을 확인했다.

final result: passed

## 48. 모바일 제품 상세정보 스프링 인터랙션

- 열린 상태 구현 캡처: `/private/tmp/seungjin-spring-product-details-open.png`
- 검증 너비: 390px
- 참고 프롬프트의 스프링 분리 아코디언 동작을 기존 `<details>` 구조와 Web Animations API로 이식했다.
- 상세정보를 열면 카드가 살짝 떠오르고 하단 패널의 간격·모서리·배경·그림자와 내용 높이·투명도·위치가 하나의 동작으로 전환된다.
- 화살표는 가벼운 오버슈트가 있는 회전으로 열림 상태를 표시하며 닫힘은 더 빠르고 단단하게 동작한다.
- 한 카드만 열리도록 관리하고 빠른 연속 전환에서도 애니메이션 상태가 꼬이지 않도록 열림·닫힘 요청을 순서대로 처리한다.
- 카드 메뉴의 `상세정보`도 같은 인터랙션으로 열리며 해당 패널이 화면 안에 오도록 기존 스크롤 동작을 유지했다.
- 방향키, Home, End로 상세정보 헤더를 이동하고 Escape로 닫을 수 있으며 모션 감소 환경에서는 즉시 전환한다.
- 열기·닫기·다른 카드 전환과 브라우저 콘솔 오류가 없음을 확인했다.

final result: passed

## 49. 모바일 카드 액체형 액션 메뉴

### 비교 환경

- 소스 비주얼: `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/TemporaryItems/NSIRD_screencaptureui_JdULhS/스크린샷 2026-08-07 오후 12.20.07.png`
- 소스 인터랙션 스펙: `/Users/kang-kyoungmo/.codex/attachments/f8e03f4f-e82e-4249-a02f-2a4b1c6f2bb2/pasted-text.txt`
- 구현 캡처: `/private/tmp/seungjin-goo-action-menu-final.png`
- 결합 비교 증거: `/private/tmp/seungjin-goo-menu-comparison.png`
- 브라우저 뷰포트: 1280 × 720 px, 모바일 앱 표시 폭 430 CSS px, device scale factor 1
- 소스 스크린샷: 852 × 1000 px를 426 × 500 px로 정규화
- 구현 집중 캡처: 430 × 380 px
- 상태: 소스는 카드 기본 상태, 구현은 `…` 메뉴 열림 상태. 카드 타이포그래피·배지·여백은 소스 이미지로, 열림 형태·모션·포커스는 참고 컴포넌트 스펙으로 비교함.

### 풀 뷰 및 집중 영역 비교

- 기존 흰색·네이비·골드 카드 스타일과 제품명, 상태 배지, 수량 정보의 위계를 유지했다.
- 메뉴는 32px 트리거의 우측 축에 맞춰 196px 표면이 아래로 펼쳐지며, 18px 모서리·은은한 표면 대비·단계적 항목 등장으로 참고 컴포넌트의 방향성을 반영했다.
- 제품 카드와 팝오버가 모두 밝은 표면이므로, 팝오버는 `#fbfcfe`와 얇은 외곽·그림자를 적용해 카드 텍스트가 비쳐 보이지 않게 했다.
- 박스 추가, 출고대기 등록·취소, 수량 변경, 등록 취소, 상세정보의 기존 레이블·아이콘·데이터 속성을 모두 유지했다.
- 폰트는 기존 Noto Sans KR을 유지하고 메뉴 항목을 12.5px/600으로 설정해 기존 카드보다 편안한 보조 계층으로 표시했다.
- 사진·로고·일러스트 추가는 없으며, 기존 제품 이미지 미등록 자리 표시자와 Tabler 메뉴 아이콘을 재사용했다.

### 기능 및 접근성 검증

- 메뉴 열기·닫기, 바깥 영역 클릭, Escape 닫기와 트리거 포커스 복귀를 확인했다.
- ArrowUp·ArrowDown 순환 이동, Home·End 이동, Tab 닫기 규칙을 확인했다.
- 다른 카드의 메뉴를 열면 기존 메뉴를 즉시 닫도록 관리했다.
- `prefers-reduced-motion: reduce`에서는 액체형 필터와 열림·닫힘 애니메이션을 제거하고 즉시 상태를 전환하도록 했다.
- 브라우저 콘솔 오류: 없음.
- `node --check frontend/mobile/mobile.js`와 `git diff --check`를 통과했다.

### 비교 이력

1. P2: 초기 구현에서 팝오버 표면이 투명해 아래 카드의 수량·구분선이 메뉴 뒤로 비쳐 보였다.
2. 수정: 액체형 배경과 팝오버 본체에 동일한 `#fbfcfe` 표면을 적용하고 외곽·그림자를 조정했다.
3. 수정 후: 메뉴 항목의 가독성, 우측 정렬, 카드 경계 내 표시, 열림·닫힘·키보드 이동을 재검증했고 추가 P0/P1/P2 이슈가 없었다.

final result: passed

## 50. 모바일 홈 메뉴 카드 중앙 정렬

- 문제 화면: `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/TemporaryItems/NSIRD_screencaptureui_hgflQ7/스크린샷 2026-08-07 오후 12.38.37.png`
- 구현 캡처: `/private/tmp/seungjin-home-card-centered.png`
- 검증 뷰포트: 1280 × 720 px, 모바일 앱 표시 폭 430 CSS px, device scale factor 1
- 카드 그리드와 아이콘·본문·상태 배지·화살표의 `align-self`를 중앙으로 통일해 위쪽으로 치우치던 내용을 카드 높이 중앙에 맞췄다.
- 메뉴 제목을 17px, -0.05em로 보정하고 한 줄로 고정해 `재고 수정 / 예외 처리`가 중간에서 줄바꿈되지 않게 했다.
- 기존 카드 높이, 설명 두 줄 제한, 상태 배지, 클릭·비활성 기능을 유지했다.
- 430px 폭에서 네 카드의 제목·설명·배지가 겹치지 않았고 브라우저 콘솔 오류가 없었다.
- `node --check frontend/mobile/mobile.js`와 `git diff --check`를 통과했다.

final result: passed

## 51. 모바일 제품명 글자 굵기

- 문제 화면: `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/TemporaryItems/NSIRD_screencaptureui_GturTw/스크린샷 2026-08-07 오후 12.39.09.png`
- 구현 캡처: `/private/tmp/seungjin-product-name-lighter-local.png`
- 결합 비교 증거: `/private/tmp/seungjin-product-name-source.png`, `/private/tmp/seungjin-product-name-lighter-crop.png`
- 검증 뷰포트: 1280 × 720px, 모바일 앱 표시 폭 430 CSS px
- 제품명의 최종 적용 굵기를 700에서 500으로 낮춰 업체명보다는 선명하고 수량·버튼보다는 편안한 중간 계층으로 조정했다.
- 출고 관리와 재고 수정 카드가 공유하는 `.shipping-product-name`에만 적용했고, 업체명·상태 배지·박스 수·출고 가능 수량·기본 버튼의 글자 굵기는 유지했다.
- 긴 제품명의 3줄 줄바꿈, 상태 배지와의 간격, 카드 표시 영역의 정렬을 동일한 430px 카드에서 확인했다.
- 브라우저 콘솔 오류는 없었다.
- `node --check frontend/mobile/mobile.js`와 `git diff --check`를 통과했다.

final result: passed

## 52. 모바일 출고 카드 이미지·상세정보 정렬

- 문제 화면: `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/TemporaryItems/NSIRD_screencaptureui_oGW2vl/스크린샷 2026-08-07 오후 12.39.19.png`
- 구현 캡처: `/private/tmp/seungjin-shipping-card-alignment-local.png`
- 결합 비교 증거: `/private/tmp/seungjin-shipping-card-alignment-source.png`, `/private/tmp/seungjin-shipping-card-alignment-crop.png`
- 검증 뷰포트: 1280 × 720px, 모바일 앱 표시 폭 430 CSS px
- 제품 이미지와 미등록 자리표시자에 `align-self: center`를 적용해 업체명·제품명·공정 배지가 만드는 헤더 높이 중앙에 오도록 조정했다.
- `제품 상세정보` 헤더에 좌측 12px, 우측 10px 안쪽 여백을 두어 카드 외곽에 붙어 보이던 텍스트와 화살표를 균형 있게 정렬했다.
- 긴 3줄 제품명, 상태 배지, 이미지 자리표시자, 수량 영역이 겹치지 않았고 상세정보 열기 동작을 유지했다.
- 브라우저 콘솔 오류는 없었다.
- `node --check frontend/mobile/mobile.js`와 `git diff --check`를 통과했다.

final result: passed

## 53. 모바일 출고 카드 등록 취소 메뉴 순서

- 문제 화면: `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/TemporaryItems/NSIRD_screencaptureui_CFSOlW/스크린샷 2026-08-07 오후 12.40.02.png`
- 구현 캡처: `/private/tmp/seungjin-shipping-menu-cancel-last-local.png`
- 결합 비교 증거: `/private/tmp/seungjin-shipping-menu-order-source.png`, `/private/tmp/seungjin-shipping-menu-cancel-last-crop.png`
- 검증 뷰포트: 1280 × 720px, 모바일 앱 표시 폭 430 CSS px
- 출고 카드 액션을 `박스 추가 → 출고대기 취소 → 수량 변경 → 상세정보 → 등록 취소` 순서로 정리했다.
- 비파괴적 정보 확인인 `상세정보`를 먼저 배치하고, 빨간색 파괴적 작업인 `등록 취소`를 구분선 아래 마지막 항목으로 유지했다.
- DOM 순서를 바꿔 화면 표시뿐 아니라 Tab·방향키 키보드 이동 순서도 동일하게 정렬했다.
- 완료 카드는 기존처럼 `상세정보`만 표시되고, 등록 중인 카드에서만 `등록 취소`가 마지막에 표시되는 조건을 유지했다.
- 브라우저 콘솔 오류는 없었다.
- `node --check frontend/mobile/mobile.js`와 `git diff --check`를 통과했다.

final result: passed

## 54. 모바일 현재 일시 `오늘` 배지 가독성

- 문제 화면: `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/TemporaryItems/NSIRD_screencaptureui_vEKE3E/스크린샷 2026-08-07 오후 12.40.13.png`
- 구현 캡처: `/private/tmp/seungjin-today-badge-readable-local.png`
- 결합 비교 증거: `/private/tmp/seungjin-today-badge-source.png`, `/private/tmp/seungjin-today-badge-readable-crop.png`
- 검증 뷰포트: 1280 × 720px, 모바일 앱 표시 폭 430 CSS px
- 기존 중간 골드 배경과 갈색 텍스트 조합을 밝은 골드 틴트 `#fff3d6`와 진한 네이비 `#173563` 조합으로 변경했다.
- 골드 테두리 `#e3b95d`를 남겨 기존 네이비·골드 디자인 언어는 유지하면서 지나치게 낡은 표시로 보이지 않게 했다.
- 텍스트와 배경의 WCAG 대비비는 11.03:1로 AA·AAA 일반 텍스트 기준을 충족한다.
- 출고 관리와 재고 수정의 동일한 현재 일시 배지에 같은 색상을 적용했다.
- 브라우저 콘솔 오류는 없었다.
- `node --check frontend/mobile/mobile.js`와 `git diff --check`를 통과했다.

final result: passed

## 55. 모바일 업체별 제품 카드 색상 구분

- 참고 컴포넌트: `/Users/kang-kyoungmo/.codex/attachments/7d99c641-191a-4296-b26c-a12ed8de1121/pasted-text.txt`
- 구현 캡처: `/private/tmp/seungjin-client-color-accents-local.png`
- 검증 뷰포트: 1280 × 720px, 모바일 앱 표시 폭 430 CSS px
- 참고 일정 카드의 카테고리 색상 바를 승진 출고 카드에 맞춰 좌측 4px 업체 액센트 바로 적용했다.
- 업체명은 같은 색상의 어두운 톤으로 표시하고, 제품명·수량·출고 버튼·상태 배지는 기존 색상을 유지해 정보 위계가 뒤집히지 않게 했다.
- `(주)`, `㈜`, `주식회사`, 공백·기호를 제거한 업체 키를 7개 팔레트에 결정적으로 매핑해 `(주)케이알`과 `케이알`처럼 표기가 달라도 같은 색상이 유지된다.
- 실제 업체 `(주)케이알`, `(주)장업시스템`, `아이원(아이텍)`, `필립텍`, `(주)정훈`의 독립적인 매핑을 확인했다.
- 모든 업체명 색상은 흰색 배경과 5.28:1 이상의 대비를 유지해 WCAG AA 일반 텍스트 기준을 충족한다.
- 출고 관리와 재고 수정의 공유 제품 카드에 동일한 규칙을 적용했다.
- 브라우저 콘솔 오류는 없었다.
- `node --check frontend/mobile/mobile.js`와 `git diff --check`를 통과했다.

final result: passed

## 56. 모바일 로그인 카드 리디자인

- 참고 컴포넌트: `/Users/kang-kyoungmo/.codex/attachments/9c021b65-b9d9-4566-8daf-0ee2b8eede5b/pasted-text.txt`
- 구현 캡처: `/private/tmp/seungjin-login-card-refresh-390x844.png`
- 오류 상태 캡처: `/private/tmp/seungjin-login-card-error-local.png`
- 검증 뷰포트: 390 × 844px, 추가 430px 모바일 앱 표시 폭 확인
- React·shadcn·Tailwind용 회원가입 예시를 현재 vanilla HTML/CSS/JS 구조로 변환하고, 다단계 가입·아바타·비밀번호 강도 기능은 로그인 업무에 맞지 않아 제외했다.
- 기존 다크 히어를 제거하고 로고·시스템명·흰색 로그인 카드를 수직 중앙 흐름으로 정리해 참고 화면의 간결한 구조를 반영했다.
- 직원번호·비밀번호·계정 저장·비밀번호 저장·자동 로그인·비밀번호 보기·관리자 로그인의 기존 ID와 이벤트를 모두 유지했다.
- 실제 작동하는 관리자 로그인을 주 버튼으로 정리하고, 기존 비활성 직원 로그인은 `직원 로그인 준비 중`으로 명확히 표시했다.
- 카드 진입 블러·슬라이드 모션, 입력 완료 체크 팝, 빈 제출 시 필드 오류 테두리와 카드 흔들림, 로그인 중 스피너를 적용했다.
- 빈 제출 시 두 필드에 `aria-invalid=true`가 적용되고, 두 값을 입력하면 오류 안내와 테두리가 자동으로 해제됨을 확인했다.
- 자동 로그인을 켜면 계정 저장과 비밀번호 저장이 함께 켜지는 기존 연동, 비밀번호 보기·숨기기 전환을 확인했다.
- `prefers-reduced-motion: reduce`에서 카드·오류·체크 모션을 제거했고, 브라우저 콘솔 오류는 없었다.
- `node --check frontend/mobile/mobile.js`와 `git diff --check`를 통과했다.

final result: passed

## 57. 모바일 제품 상세정보 아코디언 정렬

- 문제 화면: `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/TemporaryItems/NSIRD_screencaptureui_g995Tx/스크린샷 2026-08-07 오후 12.55.07.png`, `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/TemporaryItems/NSIRD_screencaptureui_Dut2YY/스크린샷 2026-08-07 오후 12.55.13.png`
- 구현 캡처: `/private/tmp/seungjin-details-accordion-aligned-local.png`
- 검증 뷰포트: 430 × 844px 모바일 표시 폭
- 펼친 상태에서 상세 영역에 음수 좌우 마진과 추가 패딩이 동시에 적용되어, 368px 본문 기준선 안쪽으로 요약·정보 영역이 346px로 한 번 더 줄어드는 문제를 확인했다.
- 펼친 상태의 이중 테두리·음수 마진·배경 패널을 제거하고, 제품 상세정보 제목·그리드·수량 영역을 모두 368px 동일 기준선에 정렬했다.
- 정보 셀은 2열 180px로 확장하고, 라벨과 값을 58px 높이 안에서 수직 중앙 정렬해 위쪽에 붙던 인상을 제거했다.
- 정보 값의 글자 굵기를 900에서 700으로 낮추고 제목·라벨을 700·600으로 정리해 카드 안의 정보 위계를 완화했다.
- 펼칠 때 전체 제품 카드가 위로 들리던 `translateY(-1px)`를 제거해 제목과 수량 정보의 화면 위치가 흔들리지 않게 했다.
- 스프링 높이·투명도·위치 애니메이션, 왼쪽 업체 색상 바, 하단 스크롤 블러, 요약 수량·출고 버튼의 기존 기능은 유지했다.
- `node --check frontend/mobile/mobile.js`와 `git diff --check`를 통과했다.

final result: passed

## 58. 재고 수정 박스 전량 이동 문구

- 문제 화면: `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/TemporaryItems/NSIRD_screencaptureui_xvTgIt/스크린샷 2026-08-07 오후 1.15.10.png`
- QR 스캔 후 하단 작업 버튼의 `현재 위치 전량`은 무엇을 전량 처리하는지 명확하지 않아 `박스 전량 이동`으로 수정했다.
- 스캐너 하단 버튼, 재고 수정 제품 카드, 단일·일괄 확인창 제목과 확정 버튼에 동일한 문구를 적용했다.
- `스캔 박스 이동`은 QR로 선택한 박스만, `박스 전량 이동`은 같은 제품·현재 보관 장소의 전체 박스를 이동하는 기존 동작을 그대로 유지했다.
- 이동 대상 선정, 보관 장소 검증, API 요청 및 완료 처리 로직은 변경하지 않았다.
- `node --check frontend/mobile/mobile.js`와 `git diff --check`를 통과했다.

final result: passed

## 59. 모바일 부분출고 카드 출고대기 등록 상태

- 문제 화면: `/var/folders/01/8n9r8rnd7dv5hkhfy_7p521w0000gn/T/TemporaryItems/NSIRD_screencaptureui_cA1Rfe/스크린샷 2026-08-07 오후 1.18.59.png`
- 구현 캡처: `/private/tmp/seungjin-shipping-pending-visibility-local.png`
- 검증 뷰포트: 430 × 844px, 추가 360 × 800px 모바일 폭 확인
- 기존에는 `부분출고` 상태가 `출고대기` 판정보다 우선 표시되어, 남은 박스가 서버의 출고대기 상태로 등록되었는지 카드에서 확인할 수 없었다.
- 부분출고 진행 상태는 그대로 유지하면서 그 아래에 `출고대기 N박스`, 일부만 등록된 경우 `출고대기 N/M박스`, 등록되지 않은 경우 `출고대기 미등록`을 별도 배지로 표시했다.
- 출고대기 등록이 완료된 카드의 주 버튼은 `추가 출고`, 미등록 카드의 주 버튼은 `출고대기 등록`으로 구분해 서버 등록 전후의 다음 동작을 명확히 했다.
- 미등록 배지는 붉은 경고 톤, 등록 완료 배지는 골드 톤, 등록 버튼은 네이비 주 버튼으로 구분했으며 기존 부분출고 파란 배지와 충돌하지 않게 했다.
- 확인창 제목도 출고대기 등록·취소, 출고 취소의 실제 동작에 맞게 구분했다.
- 430px와 360px에서 카드 좌우 정렬, 상태 영역, 수량 영역, 버튼에 가로 넘침이 없음을 확인했다.
- 출고대기 판정, 서버 요청, 출고 완료 및 부분출고 계산 로직은 기존 흐름을 유지했다.
- `node --check frontend/mobile/mobile.js`와 `git diff --check`를 통과했다.

final result: passed
